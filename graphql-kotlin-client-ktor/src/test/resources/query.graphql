# Write your query or mutation here
query AllStarWarsFilms($orderBy: FilmOrderBy) {
    allFilms(orderBy:$orderBy){
        id
        title
        director
        characters {
            name
            homeworld{
                name
            }
        }
    }
}